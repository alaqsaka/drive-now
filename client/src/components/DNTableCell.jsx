import { TableCell } from "@mui/material";
import { withStyles } from "@mui/styles";
import React from "react";

const DNTableCell = withStyles({
	root: {
		fontSize: "16px",
	},
})(TableCell);

export default DNTableCell;
